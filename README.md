```cpp
bashrpc-mdse/
├── .bashmainrpc           # Core BashRPC daemon
├── model.json             # Model definition (services & methods)
├── .env                   # Secrets (GPT key, endpoints)
├── Dockerfile             # Containerization
└── docker-compose.yml     # Optional full stack
